'use strict';


// Listens to all events
// Logs event, timestamp, and payload for every event
















// Listens to ALL events in the Event Pool.
// Logs a timestamp and the payload of every event.

// const eventPool = require('./globalEvent.js');


// eventPool.on('pickup', (payload) => {
//   const event = {
//     'event': 'pickup',
//     'time': new Date(),
//     payload,
//   };
//   console.log(event);
// });

// eventPool.on('transit', (payload) => {
//   const event = {
//     'event': 'in-transit',
//     'time': new Date(),
//     payload,
//   };
//   console.log(event);
// });

// eventPool.on('delivered', (payload) => {
//   const event = {
//     'event': 'delivered',
//     'time': new Date(),
//     payload,
//   };
//   console.log(event);
// });

// // .on for each function, calls function that logs each event


// // }